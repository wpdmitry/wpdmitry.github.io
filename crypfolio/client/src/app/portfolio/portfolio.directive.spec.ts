import { PortfolioDirective } from './portfolio.directive';

describe('PortfolioDirective', () => {
  it('should create an instance', () => {
    const directive = new PortfolioDirective();
    expect(directive).toBeTruthy();
  });
});
